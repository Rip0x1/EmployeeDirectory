@page
@model ErrorModel
@{
    ViewData["Title"] = "Ошибка";
}

<div class="container-fluid">
    <div class="row justify-content-center align-items-center" style="min-height: 70vh;">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <div class="card-body text-center text-white p-5">
                    <div class="mb-4">
                        <i class="fas fa-exclamation-triangle" style="font-size: 5rem; color: #ffc107;"></i>
                    </div>
                    <h1 class="display-4 fw-bold mb-3">Ошибка</h1>
                    <h3 class="mb-4">Произошла ошибка при обработке вашего запроса</h3>
                    
                    @if (Model.ShowRequestId)
                    {
                        <div class="bg-white bg-opacity-10 rounded p-3 mb-4">
                            <p class="mb-1"><strong>ID запроса:</strong></p>
                            <code class="text-white">@Model.RequestId</code>
                        </div>
                    }
                    
                    <div class="bg-white bg-opacity-10 rounded p-4 mb-4">
                        <h4 class="mb-3"><i class="fas fa-info-circle me-2"></i>Режим разработки</h4>
                        <p class="mb-3">
                            Переключение в режим <strong>Development</strong> отображает подробную информацию об ошибке.
                        </p>
                        <p class="mb-0 small">
                            <strong>Внимание:</strong> Режим разработки не должен быть включен в production окружении.
                            Это может привести к отображению конфиденциальной информации пользователям.
                        </p>
                    </div>
                    
                    <div class="d-flex gap-3 justify-content-center">
                        <a href="/" class="btn btn-light btn-lg">
                            <i class="fas fa-home me-2"></i>На главную
                        </a>
                        <a href="javascript:history.back()" class="btn btn-outline-light btn-lg">
                            <i class="fas fa-arrow-left me-2"></i>Назад
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        background: linear-gradient(135deg, #56ab2f 0%, #a8e6cf 50%, #11998e 100%);
        min-height: 100vh;
    }
</style>
